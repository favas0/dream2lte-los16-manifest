diff --git a/tools/event_log_tags.py b/tools/event_log_tags.py
index 645839ec2..fa2a9233e 100644
--- a/tools/event_log_tags.py
+++ b/tools/event_log_tags.py
@@ -12,8 +12,11 @@
 # See the License for the specific language governing permissions and
 # limitations under the License.
 
+from __future__ import print_function
+
 """A module for reading and parsing event-log-tags files."""
 
+import io
 import re
 import sys
 
@@ -54,8 +57,8 @@ class TagFile(object):
 
     if file_object is None:
       try:
-        file_object = open(filename, "rb")
-      except (IOError, OSError), e:
+        file_object = io.open(filename, "r", encoding="utf-8")
+      except (IOError, OSError) as e:
         self.AddError(str(e))
         return
 
@@ -100,7 +103,7 @@ class TagFile(object):
 
         self.tags.append(Tag(tag, tagname, description,
                              self.filename, self.linenum))
-    except (IOError, OSError), e:
+    except (IOError, OSError) as e:
       self.AddError(str(e))
 
 
@@ -127,9 +130,9 @@ def WriteOutput(output_file, data):
       out = sys.stdout
       output_file = "<stdout>"
     else:
-      out = open(output_file, "wb")
+      out = io.open(output_file, "w", encoding="utf-8")
     out.write(data)
     out.close()
-  except (IOError, OSError), e:
-    print >> sys.stderr, "failed to write %s: %s" % (output_file, e)
+  except (IOError, OSError) as e:
+    print("failed to write %s: %s" % (output_file, e), file=sys.stderr)
     sys.exit(1)
diff --git a/tools/fs_config/fs_config_generator.py b/tools/fs_config/fs_config_generator.py
index 1f6de80bf..404712ebb 100755
--- a/tools/fs_config/fs_config_generator.py
+++ b/tools/fs_config/fs_config_generator.py
@@ -11,7 +11,10 @@ Further documentation can be found in the README.
 """
 
 import argparse
-import ConfigParser
+try:
+    import ConfigParser
+except ImportError:
+    import configparser as ConfigParser
 import re
 import sys
 import textwrap
@@ -423,7 +426,7 @@ class AIDHeaderParser(object):
         # tuplefy the lists since range() does not like them mutable.
         self._oem_ranges = [
             AIDHeaderParser._convert_lst_to_tup(k, v)
-            for k, v in self._oem_ranges.iteritems()
+            for k, v in self._oem_ranges.items()
         ]
 
         # Check for overlapping ranges
@@ -435,12 +438,13 @@ class AIDHeaderParser(object):
 
         # No core AIDs should be within any oem range.
         for aid in self._aid_value_to_name:
+            aid_int = int(aid, 0)
 
-            if Utils.in_any_range(aid, self._oem_ranges):
+            if Utils.in_any_range(aid_int, self._oem_ranges):
                 name = self._aid_value_to_name[aid]
                 raise ValueError(
                     'AID "%s" value: %u within reserved OEM Range: "%s"' %
-                    (name, aid, str(self._oem_ranges)))
+                    (name, aid_int, str(self._oem_ranges)))
 
     @property
     def oem_ranges(self):
@@ -1037,14 +1041,14 @@ class FSConfigGen(BaseGenerator):
 
         expanded = fmt % (mode, user, group, caps, path)
 
-        print FSConfigGen._FILE_COMMENT % fname
-        print '    ' + expanded
+        print(FSConfigGen._FILE_COMMENT % fname)
+        print('    ' + expanded)
 
     @staticmethod
     def _gen_inc():
         """Generate the include header lines and print to stdout."""
         for include in FSConfigGen._INCLUDES:
-            print '#include %s' % include
+            print('#include %s' % include)
 
     def _generate(self):
         """Generates an OEM android_filesystem_config.h header file to stdout.
@@ -1055,7 +1059,7 @@ class FSConfigGen(BaseGenerator):
                 entries.
             aids ([AIDS]): A list of AID objects for Android Id entries.
         """
-        print FSConfigGen._GENERATED
+        print(FSConfigGen._GENERATED)
         print
 
         FSConfigGen._gen_inc()
@@ -1072,39 +1076,39 @@ class FSConfigGen(BaseGenerator):
         if are_aids:
             for aid in aids:
                 # use the preserved _path value
-                print FSConfigGen._FILE_COMMENT % aid.found
-                print FSConfigGen._GENERIC_DEFINE % (aid.identifier, aid.value)
+                print(FSConfigGen._FILE_COMMENT % aid.found)
+                print(FSConfigGen._GENERIC_DEFINE % (aid.identifier, aid.value))
 
             print
 
         if not are_dirs:
-            print FSConfigGen._DEFINE_NO_DIRS + '\n'
+            print(FSConfigGen._DEFINE_NO_DIRS + '\n')
 
         if not are_files:
-            print FSConfigGen._DEFINE_NO_FILES + '\n'
+            print(FSConfigGen._DEFINE_NO_FILES + '\n')
 
         if not are_files and not are_dirs and not are_aids:
             return
 
         if are_files:
-            print FSConfigGen._OPEN_FILE_STRUCT
+            print(FSConfigGen._OPEN_FILE_STRUCT)
             for fs_config in files:
                 self._to_fs_entry(fs_config)
 
             if not are_dirs:
-                print FSConfigGen._IFDEF_ANDROID_FILESYSTEM_CONFIG_DEVICE_DIRS
+                print(FSConfigGen._IFDEF_ANDROID_FILESYSTEM_CONFIG_DEVICE_DIRS)
                 print(
                     '    ' +
                     FSConfigGen._NO_ANDROID_FILESYSTEM_CONFIG_DEVICE_DIRS_ENTRY)
-                print FSConfigGen._ENDIF
-            print FSConfigGen._CLOSE_FILE_STRUCT
+                print(FSConfigGen._ENDIF)
+            print(FSConfigGen._CLOSE_FILE_STRUCT)
 
         if are_dirs:
-            print FSConfigGen._OPEN_DIR_STRUCT
+            print(FSConfigGen._OPEN_DIR_STRUCT)
             for dir_entry in dirs:
                 self._to_fs_entry(dir_entry)
 
-            print FSConfigGen._CLOSE_FILE_STRUCT
+            print(FSConfigGen._CLOSE_FILE_STRUCT)
 
 
 @generator('aidarray')
@@ -1142,20 +1146,20 @@ class AIDArrayGen(BaseGenerator):
 
         hdr = AIDHeaderParser(args['hdrfile'])
 
-        print AIDArrayGen._GENERATED
+        print(AIDArrayGen._GENERATED)
         print
-        print AIDArrayGen._INCLUDE
+        print(AIDArrayGen._INCLUDE)
         print
-        print AIDArrayGen._STRUCT_FS_CONFIG
+        print(AIDArrayGen._STRUCT_FS_CONFIG)
         print
-        print AIDArrayGen._OPEN_ID_ARRAY
+        print(AIDArrayGen._OPEN_ID_ARRAY)
 
         for aid in hdr.aids:
-            print AIDArrayGen._ID_ENTRY % (aid.friendly, aid.identifier)
+            print(AIDArrayGen._ID_ENTRY % (aid.friendly, aid.identifier))
 
-        print AIDArrayGen._CLOSE_FILE_STRUCT
+        print(AIDArrayGen._CLOSE_FILE_STRUCT)
         print
-        print AIDArrayGen._COUNT
+        print(AIDArrayGen._COUNT)
         print
 
 
@@ -1207,15 +1211,15 @@ class OEMAidGen(BaseGenerator):
                                     hdr_parser.oem_ranges,
                                     args['allow_legacy_aids'])
 
-        print OEMAidGen._GENERATED
+        print(OEMAidGen._GENERATED)
 
-        print OEMAidGen._FILE_IFNDEF_DEFINE
+        print(OEMAidGen._FILE_IFNDEF_DEFINE)
 
         for aid in parser.aids:
             self._print_aid(aid)
             print
 
-        print OEMAidGen._FILE_ENDIF
+        print(OEMAidGen._FILE_ENDIF)
 
     def _print_aid(self, aid):
         """Prints a valid #define AID identifier to stdout.
@@ -1227,10 +1231,10 @@ class OEMAidGen(BaseGenerator):
         # print the source file location of the AID
         found_file = aid.found
         if found_file != self._old_file:
-            print OEMAidGen._FILE_COMMENT % found_file
+            print(OEMAidGen._FILE_COMMENT % found_file)
             self._old_file = found_file
 
-        print OEMAidGen._GENERIC_DEFINE % (aid.identifier, aid.value)
+        print(OEMAidGen._GENERIC_DEFINE % (aid.identifier, aid.value))
 
 
 @generator('passwd')
@@ -1310,7 +1314,7 @@ class PasswdGen(BaseGenerator):
         except ValueError as exception:
             sys.exit(exception)
 
-        print "%s::%s:%s::/:/system/bin/sh" % (logon, uid, uid)
+        print("%s::%s:%s::/:/system/bin/sh" % (logon, uid, uid))
 
 
 @generator('group')
@@ -1335,7 +1339,7 @@ class GroupGen(PasswdGen):
         except ValueError as exception:
             sys.exit(exception)
 
-        print "%s::%s:" % (logon, uid)
+        print("%s::%s:" % (logon, uid))
 
 
 def main():
@@ -1349,7 +1353,7 @@ def main():
     gens = generator.get()
 
     # for each gen, instantiate and add them as an option
-    for name, gen in gens.iteritems():
+    for name, gen in gens.items():
 
         generator_option_parser = subparser.add_parser(name, help=gen.__doc__)
         generator_option_parser.set_defaults(which=name)
diff --git a/tools/java-event-log-tags.py b/tools/java-event-log-tags.py
index 37cd71220..52ab16151 100755
--- a/tools/java-event-log-tags.py
+++ b/tools/java-event-log-tags.py
@@ -14,6 +14,8 @@
 # See the License for the specific language governing permissions and
 # limitations under the License.
 
+from __future__ import print_function, unicode_literals
+
 """
 Usage: java-event-log-tags.py [-o output_file] <input_file> <merged_tags_file>
 
@@ -23,8 +25,8 @@ tags in the given input file.
 -h to display this usage message and exit.
 """
 
-import cStringIO
 import getopt
+import io
 import os
 import os.path
 import re
@@ -34,36 +36,41 @@ import event_log_tags
 
 output_file = None
 
+def _text_arg(value):
+  if isinstance(value, bytes):
+    return value.decode("utf-8")
+  return value
+
 try:
   opts, args = getopt.getopt(sys.argv[1:], "ho:")
-except getopt.GetoptError, err:
-  print str(err)
-  print __doc__
+except getopt.GetoptError as err:
+  print(str(err))
+  print(__doc__)
   sys.exit(2)
 
 for o, a in opts:
   if o == "-h":
-    print __doc__
+    print(__doc__)
     sys.exit(2)
   elif o == "-o":
     output_file = a
   else:
-    print >> sys.stderr, "unhandled option %s" % (o,)
+    print("unhandled option %s" % (o,), file=sys.stderr)
     sys.exit(1)
 
 if len(args) != 1 and len(args) != 2:
-  print "need one or two input files, not %d" % (len(args),)
-  print __doc__
+  print("need one or two input files, not %d" % (len(args),))
+  print(__doc__)
   sys.exit(1)
 
-fn = args[0]
+fn = _text_arg(args[0])
 tagfile = event_log_tags.TagFile(fn)
 
 if len(args) > 1:
   # Load the merged tag file (which should have numbers assigned for all
   # tags.  Use the numbers from the merged file to fill in any missing
   # numbers from the input file.
-  merged_fn = args[1]
+  merged_fn = _text_arg(args[1])
   merged_tagfile = event_log_tags.TagFile(merged_fn)
   merged_by_name = dict([(t.tagname, t) for t in merged_tagfile.tags])
   for t in tagfile.tags:
@@ -81,7 +88,7 @@ else:
   # numbers
   for t in tagfile.tags:
     if t.tagnum is None:
-      tagfilef.AddError("tag \"%s\" has no number" % (tagname,), tag.linenum)
+      tagfile.AddError("tag \"%s\" has no number" % (t.tagname,), t.linenum)
 
 if "java_package" not in tagfile.options:
   tagfile.AddError("java_package option not specified", linenum=0)
@@ -92,10 +99,10 @@ if "javadoc_hide" in tagfile.options:
 
 if tagfile.errors:
   for fn, ln, msg in tagfile.errors:
-    print >> sys.stderr, "%s:%d: error: %s" % (fn, ln, msg)
+    print("%s:%d: error: %s" % (fn, ln, msg), file=sys.stderr)
   sys.exit(1)
 
-buffer = cStringIO.StringIO()
+buffer = io.StringIO()
 buffer.write("/* This file is auto-generated.  DO NOT MODIFY.\n"
              " * Source file: %s\n"
              " */\n\n" % (fn,))
diff --git a/tools/merge-event-log-tags.py b/tools/merge-event-log-tags.py
index 64bad3f81..a544ee0ab 100755
--- a/tools/merge-event-log-tags.py
+++ b/tools/merge-event-log-tags.py
@@ -14,6 +14,8 @@
 # See the License for the specific language governing permissions and
 # limitations under the License.
 
+from __future__ import print_function
+
 """
 Usage: merge-event-log-tags.py [-o output_file] [input_files...]
 
@@ -24,12 +26,12 @@ and fails if they do.
 -h to display this usage message and exit.
 """
 
-import cStringIO
 import getopt
 try:
   import hashlib
 except ImportError:
   import md5 as hashlib
+import io
 import struct
 import sys
 
@@ -48,21 +50,21 @@ ASSIGN_LIMIT = 1000000
 
 try:
   opts, args = getopt.getopt(sys.argv[1:], "ho:m:")
-except getopt.GetoptError, err:
-  print str(err)
-  print __doc__
+except getopt.GetoptError as err:
+  print(str(err))
+  print(__doc__)
   sys.exit(2)
 
 for o, a in opts:
   if o == "-h":
-    print __doc__
+    print(__doc__)
     sys.exit(2)
   elif o == "-o":
     output_file = a
   elif o == "-m":
     pre_merged_file = a
   else:
-    print >> sys.stderr, "unhandled option %s" % (o,)
+    print("unhandled option %s" % (o,), file=sys.stderr)
     sys.exit(1)
 
 # Restrictions on tags:
@@ -133,18 +135,18 @@ for fn in args:
 
 if errors:
   for fn, ln, msg in errors:
-    print >> sys.stderr, "%s:%d: error: %s" % (fn, ln, msg)
+    print("%s:%d: error: %s" % (fn, ln, msg), file=sys.stderr)
   sys.exit(1)
 
 if warnings:
   for fn, ln, msg in warnings:
-    print >> sys.stderr, "%s:%d: warning: %s" % (fn, ln, msg)
+    print("%s:%d: warning: %s" % (fn, ln, msg), file=sys.stderr)
 
 # Python's hash function (a) isn't great and (b) varies between
 # versions of python.  Using md5 is overkill here but is the same from
 # platform to platform and speed shouldn't matter in practice.
-def hashname(str):
-  d = hashlib.md5(str).digest()[:4]
+def hashname(name):
+  d = hashlib.md5(name.encode("utf-8")).digest()[:4]
   return struct.unpack("!I", d)[0]
 
 # Assign a tag number to all the entries that say they want one
@@ -154,14 +156,14 @@ def hashname(str):
 # If we were provided pre-merged tags (w/ the -m option), then don't
 # ever try to allocate one, just fail if we don't have a number
 
-for name, t in sorted(by_tagname.iteritems()):
+for name, t in sorted(by_tagname.items()):
   if t.tagnum is None:
     if pre_merged_tags:
       try:
         t.tagnum = pre_merged_tags[t.tagname]
       except KeyError:
-        print >> sys.stderr, ("Error: Tag number not defined for tag `%s'."
-            +" Have you done a full build?") % t.tagname
+        print(("Error: Tag number not defined for tag `%s'."
+            + " Have you done a full build?") % t.tagname, file=sys.stderr)
         sys.exit(1)
     else:
       while True:
@@ -174,8 +176,8 @@ for name, t in sorted(by_tagname.iteritems()):
 
 # by_tagnum should be complete now; we've assigned numbers to all tags.
 
-buffer = cStringIO.StringIO()
-for n, t in sorted(by_tagnum.iteritems()):
+buffer = io.StringIO()
+for n, t in sorted(by_tagnum.items()):
   if t.description:
     buffer.write("%d %s %s\n" % (t.tagnum, t.tagname, t.description))
   else:
diff --git a/tools/normalize_path.py b/tools/normalize_path.py
index 6c4d5486d..363df1f1f 100755
--- a/tools/normalize_path.py
+++ b/tools/normalize_path.py
@@ -22,8 +22,8 @@ import sys
 
 if len(sys.argv) > 1:
   for p in sys.argv[1:]:
-    print os.path.normpath(p)
+    print(os.path.normpath(p))
   sys.exit(0)
 
 for line in sys.stdin:
-  print os.path.normpath(line.strip())
+  print(os.path.normpath(line.strip()))
